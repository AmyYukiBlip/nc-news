import { useState } from "react";
import "./App.css";

function App() {
  return (
    <>
      
    </>
  );
}

export default App;

// const [count, setCount] = useState(0)

//  <button onClick={() => setCount((count) => count + 1)}>
// count is {count}
// </button>
